export type QuestType = {
  id?: string,
  // userId?: string,
  domain?: string,
  title?: string,
  description?: string,
  xpReward?: number,
  durationMinutes?: number,
  status?: number,
  scheduledAt?: string,
  completedAt?: string,
  createdAt?: string
}